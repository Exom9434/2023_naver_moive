import torch
import torch.nn as nn
import torch.optim as optim
from lstm import Model

class Train:
    def __init__(self,loss_func, optimizer):
        self.loss_func = 
        self.opimizer = nn.optimizer
        
    def train(self, model, num_epochs):
        model.train()
        for i in range(num_epochs):
                model.